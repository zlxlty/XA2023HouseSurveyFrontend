import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'cc.hyperex.mirro',
  appName: 'Mirro',
  webDir: 'dist',
  bundledWebRuntime: false,
}

export default config
